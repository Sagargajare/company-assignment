# Docker Compose Environment Variables
# These variables are used by docker-compose.yml for PostgreSQL configuration

# Database Configuration (used by Docker Compose)
DB_USER=test_owner
DB_PASSWORD=npg_S6AhEUx8TsIW
DB_HOST=ep-little-moon-a8efsckc-pooler.eastus2.azure.neon.tech
DB_PORT=5432
DB_NAME=test
